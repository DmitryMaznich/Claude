# Варианты деплоя на cPanel (для обхода файрвола)

## Текущая ситуация
У вас настроено **5 различных методов** деплоя на cPanel. Все они доступны в разделе Actions вашего GitHub репозитория:
https://github.com/DmitryMaznich/Claude/actions

---

## 🚀 Доступные методы деплоя

### 1. **Deploy to cPanel** (основной, FTPS порт 21)
- **Файл:** `.github/workflows/deploy.yml`
- **Когда использовать:** По умолчанию, если FTPS не заблокирован
- **Запуск:** Автоматически при push в main
- **Требует secrets:** FTP_SERVER, FTP_USERNAME, FTP_PASSWORD

---

### 2. **Deploy via SSH/Rsync** ⭐ РЕКОМЕНДУЕТСЯ при блокировке FTP
- **Файл:** `.github/workflows/deploy-ssh.yml`
- **Когда использовать:** Когда FTP заблокирован, использует SSH (порт 22)
- **Запуск:** Вручную через GitHub Actions
- **Требует secrets:**
  - `SSH_PRIVATE_KEY` - приватный SSH ключ
  - `FTP_SERVER` - адрес сервера
  - `FTP_USERNAME` - имя пользователя

**Как настроить SSH ключ:**
1. Сгенерируйте SSH ключ на своем компьютере:
   ```bash
   ssh-keygen -t rsa -b 4096 -C "deploy@github"
   ```
2. Добавьте публичный ключ (~/.ssh/id_rsa.pub) в cPanel → SSH Access → Manage SSH Keys
3. Добавьте приватный ключ в GitHub Secrets как `SSH_PRIVATE_KEY`

---

### 3. **Deploy via SCP** ⭐ ПРОСТОЙ вариант для SSH
- **Файл:** `.github/workflows/deploy-scp.yml`
- **Когда использовать:** Альтернатива rsync, использует SSH (порт 22)
- **Запуск:** Вручную через GitHub Actions
- **Требует secrets:** FTP_SERVER, FTP_USERNAME, FTP_PASSWORD (или SSH_PRIVATE_KEY)
- **Плюсы:** Проще rsync, работает с паролем

---

### 4. **Deploy FTP Alternative Ports** 🔧 Пробует разные порты
- **Файл:** `.github/workflows/deploy-alternative-ports.yml`
- **Когда использовать:** Когда стандартный порт 21 заблокирован
- **Запуск:** Вручную через GitHub Actions
- **Требует secrets:** FTP_SERVER, FTP_USERNAME, FTP_PASSWORD
- **Что делает:**
  - Сначала пробует порт 2222
  - Если не удается, пробует порт 990 (FTPS implicit)

---

### 5. **Trigger cPanel Git Deploy** 🎯 Использует встроенный Git cPanel
- **Файл:** `.github/workflows/trigger-cpanel-git.yml`
- **Когда использовать:** Самый надежный вариант через файрвол
- **Запуск:** Вручную через GitHub Actions
- **Требует secrets:** `CPANEL_GIT_WEBHOOK_URL`

**Как настроить cPanel Git:**
1. В cPanel → Git Version Control → Create
2. Clone URL: `https://github.com/DmitryMaznich/Claude.git`
3. Repository Path: `/home/yourusername/public_html`
4. Branch: `main`
5. После создания, скопируйте webhook URL
6. Добавьте его в GitHub Secrets как `CPANEL_GIT_WEBHOOK_URL`

---

## 📋 Как использовать ручные деплои

1. Перейдите на: https://github.com/DmitryMaznich/Claude/actions
2. Выберите нужный workflow (Deploy via SSH/Rsync, Deploy via SCP и т.д.)
3. Нажмите "Run workflow" → выберите ветку `main` → "Run workflow"
4. Дождитесь завершения (зеленая галочка = успех, красный крестик = ошибка)

---

## 🔑 Необходимые GitHub Secrets

Проверьте, что у вас настроены следующие секреты:
https://github.com/DmitryMaznich/Claude/settings/secrets/actions

### Для FTP/FTPS методов:
- `FTP_SERVER` - адрес сервера (например: ftp.smart-wash.si)
- `FTP_USERNAME` - имя пользователя FTP
- `FTP_PASSWORD` - пароль FTP

### Для SSH методов:
- `SSH_PRIVATE_KEY` - приватный SSH ключ (весь контент файла ~/.ssh/id_rsa)

### Для cPanel Git:
- `CPANEL_GIT_WEBHOOK_URL` - webhook URL из cPanel Git Version Control

---

## 🎯 Рекомендация при блокировке файрвола

**Попробуйте в таком порядке:**

1. **Deploy via SCP** - самый простой, работает с паролем через SSH (порт 22)
2. **Deploy via SSH/Rsync** - более продвинутый, требует SSH ключ
3. **Trigger cPanel Git Deploy** - самый надежный, использует встроенную функцию cPanel
4. **Deploy FTP Alternative Ports** - если SSH тоже заблокирован

---

## 🔧 Отладка проблем

Если деплой не работает:
1. Проверьте логи в GitHub Actions (кликните на failed workflow)
2. Убедитесь, что все secrets правильно настроены
3. Проверьте, что порты не заблокированы:
   ```bash
   telnet your-server.com 21   # FTP
   telnet your-server.com 22   # SSH
   telnet your-server.com 990  # FTPS implicit
   ```

---

## 📝 Активация автоматического деплоя

Чтобы сделать любой метод автоматическим при push:

1. Откройте нужный `.github/workflows/deploy-*.yml`
2. Раскомментируйте:
   ```yaml
   push:
     branches:
       - main
   ```
3. Закомментируйте в основном `deploy.yml` автоматический запуск
4. Сделайте commit и push
