# 🚀 Deploy на cPanel - Пошаговая Инструкция

## ✅ Как это работает

```
Вы → deploy.bat → GitHub → Ручная загрузка на cPanel
```

Код сохраняется в GitHub для версионирования, затем загружается на cPanel вручную.

---

## 📋 Настройка (один раз)

### Шаг 1: Получите FTP данные из cPanel

1. Откройте ваш **cPanel**
2. Найдите раздел **"FTP Accounts"** или **"FTP Access"**
3. Запишите или создайте FTP аккаунт:
   - **FTP Server**: обычно это ваш домен (например: `ftp.smartwash.si`)
   - **Username**: ваш FTP логин (например: `smartwash@smartwash.si`)
   - **Password**: ваш FTP пароль

### Шаг 2: Добавьте секреты в GitHub

1. Откройте ваш репозиторий на GitHub: https://github.com/DmitryMaznich/Claude
2. Перейдите в **Settings** (настройки репозитория)
3. В левом меню найдите **"Secrets and variables"** → **"Actions"**
4. Нажмите **"New repository secret"** и добавьте 3 секрета:

**Секрет 1:**
- Name: `FTP_SERVER`
- Value: `ftp.smartwash.si` (ваш FTP сервер)

**Секрет 2:**
- Name: `FTP_USERNAME`
- Value: `smartwash@smartwash.si` (ваш FTP логин)

**Секрет 3:**
- Name: `FTP_PASSWORD`
- Value: `ваш_фтп_пароль` (ваш FTP пароль)

### Шаг 3: Готово! 🎉

Теперь автоматический деплой настроен!

---

## 🚀 Использование

### Простой способ - запустите deploy.bat

```bash
deploy.bat
```

Скрипт:
1. Спросит commit message
2. Добавит все изменения
3. Создаст коммит
4. Запушит на GitHub
5. **GitHub Actions автоматически задеплоит на cPanel!**

### Ручной способ

```bash
git add .
git commit -m "Update website design"
git push origin main
```

После пуша GitHub Actions автоматически:
- Скачает ваш код
- Загрузит на cPanel через FTP
- Деплой готов!

---

## 🔍 Проверка деплоя

### Посмотрите статус в GitHub

1. Откройте ваш репозиторий: https://github.com/DmitryMaznich/Claude
2. Перейдите во вкладку **"Actions"**
3. Увидите список всех деплоев:
   - ✅ Зеленая галочка = деплой успешен
   - ❌ Красный крестик = ошибка (смотрите логи)
4. Нажмите на любой деплой чтобы увидеть детальные логи

### Проверьте файлы в cPanel

1. Откройте **File Manager** в cPanel
2. Перейдите в папку `public_html`
3. Убедитесь что файлы обновились (смотрите дату изменения)

---

## ⚙️ Настройка деплоя

### Изменить путь на сервере

Откройте файл `.github/workflows/deploy.yml` и измените строку:
```yaml
server-dir: /public_html/
```

На ваш путь, например:
```yaml
server-dir: /public_html/site/
```

### Исключить файлы из деплоя

В том же файле `.github/workflows/deploy.yml` в секции `exclude:` добавьте файлы которые не нужно загружать:
```yaml
exclude: |
  **/.git*
  **/node_modules/**
  .env
  ваш_файл.txt
```

---

## 🔧 Troubleshooting

### ❌ Деплой падает с ошибкой FTP

**Проблема**: Connection timeout или authentication failed

**Решение**:
1. Проверьте что FTP секреты правильные в GitHub Settings → Secrets
2. Проверьте что FTP аккаунт активен в cPanel
3. Убедитесь что FTP сервер доступен (не блокирован файрволом)

### ❌ Файлы не появляются на сайте

**Проблема**: Деплой успешен но файлы не видны

**Решение**:
1. Проверьте правильность пути в `server-dir` (должен быть `/public_html/`)
2. Проверьте права доступа к папке в cPanel File Manager
3. Очистите кеш браузера (Ctrl+F5)

### ❌ GitHub Actions не запускается

**Проблема**: При пуше ничего не происходит

**Решение**:
1. Проверьте что файл `.github/workflows/deploy.yml` существует
2. Проверьте что вы пушите в ветку `main` (не в другую)
3. Перейдите в GitHub → Settings → Actions → разрешите GitHub Actions

---

## 📁 Важные файлы

- `.github/workflows/deploy.yml` - настройка автоматического деплоя
- `deploy.bat` - скрипт для быстрого деплоя
- `.gitignore` - файлы которые не коммитятся

---

## 💡 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть последние коммиты
git log --oneline -5

# Отменить последний коммит (если еще не запушили)
git reset HEAD~1

# Посмотреть что изменилось
git diff

# Посмотреть ветки
git branch -a
```

---

## 🎯 Преимущества GitHub Actions

✅ **Автоматический деплой** - пушите код, всё остальное сделается само
✅ **Логи деплоя** - видите что происходит, легко найти ошибки
✅ **Не зависит от cPanel** - работает с любым хостингом
✅ **Бесплатно** - для публичных репозиториев
✅ **Надежно** - GitHub сам следит за деплоем

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в GitHub Actions
2. Проверьте FTP секреты в GitHub Settings
3. Убедитесь что FTP аккаунт работает (подключитесь через FileZilla)
