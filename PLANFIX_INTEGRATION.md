# Интеграция с Планфикс CRM

Эта интеграция автоматически создает задачи в Планфикс для каждой сессии чата и логирует все сообщения.

## 🎯 Возможности

- ✅ **Автоматическое создание задач** при начале новой сессии чата
- ✅ **Логирование всех сообщений** как комментариев к задаче
- ✅ **Отслеживание роли отправителя** (клиент, AI ассистент, оператор)
- ✅ **Автоматическое закрытие задач** при завершении сессии
- ✅ **Сводка сессии** с длительностью и количеством сообщений
- ✅ **Теги и метаданные** (язык клиента, режим чата)

## 📋 Что логируется в Планфикс

### При создании задачи:
- ID сессии
- Имя клиента (или Customer№)
- Язык общения
- Дата и время начала сессии
- Первое сообщение клиента

### Во время сессии:
- Все сообщения клиента с временными метками
- Все ответы AI ассистента
- Все сообщения оператора (если подключался)
- Фотографии (как ссылки)

### При закрытии задачи:
- Общая длительность сессии
- Количество сообщений
- Информация о подключении оператора
- Дата и время завершения

## 🔧 Настройка

### Шаг 1: Получение API токена в Планфикс

1. Войдите в ваш аккаунт Планфикс
2. Перейдите: **Настройки аккаунта** → **API доступ** → **REST API**
3. Нажмите **"Создать токен"** или используйте существующий
4. Скопируйте токен (он выглядит примерно так: `abc123def456ghi789...`)

### Шаг 2: Найдите ID проекта

1. Откройте проект в Планфиксе, куда будут создаваться задачи
2. ID проекта можно найти в URL: `https://ваш-аккаунт.planfix.ru/project/12345`
3. В этом примере ID проекта = `12345`

### Шаг 3: Настройка переменных окружения

Откройте файл `.env` и заполните параметры Планфикса:

```bash
# Planfix CRM Integration
PLANFIX_ENABLED=true                    # Включить интеграцию (true/false)
PLANFIX_ACCOUNT=ваш-аккаунт            # Ваш поддомен (например: mycompany)
PLANFIX_API_TOKEN=ваш-токен-здесь      # API токен из шага 1
PLANFIX_PROJECT_ID=12345                # ID проекта из шага 2
```

**Пример:**
```bash
PLANFIX_ENABLED=true
PLANFIX_ACCOUNT=smartwash
PLANFIX_API_TOKEN=abc123def456ghi789jkl012mno345pqr678
PLANFIX_PROJECT_ID=54321
```

### Шаг 4: Перезапуск сервера

После изменения `.env` файла перезапустите сервер:

```bash
npm restart
```

или если используете nodemon:

```bash
npm run dev
```

## ✅ Проверка работы интеграции

При запуске сервера вы должны увидеть в логах:

```
✅ Planfix integration initialized
   Account: ваш-аккаунт
   Project: 12345
```

Когда начинается новая сессия:

```
✅ Session session-abc123 linked to Planfix task 67890
📎 Task URL: https://ваш-аккаунт.planfix.ru/task/67890
```

## 📊 Структура задачи в Планфиксе

### Название задачи:
```
Чат: Иван Иванов (Russian)
```
или
```
Чат: Customer42 (Slovenian)
```

### Описание задачи:
```
🆕 НОВАЯ СЕССИЯ ЧАТА

ID сессии: session-abc123
Клиент: Иван Иванов
Язык: Russian
Создана: 19.02.2026, 14:30

Первое сообщение:
Здравствуйте, как открыть дверь?

---
Эта задача автоматически создана из чата на сайте.
Все сообщения будут логироваться как комментарии ниже.
```

### Комментарии (сообщения):
```
👤 Иван Иванов | 14:30:15
Как работает стиральная машина?

🤖 AI Assistant | 14:30:18
Для использования машины нужно вставить 5 жетонов...

👨‍💼 Оператор | 14:35:20
Могу помочь вам прямо сейчас!
```

### Итоговая сводка при закрытии:
```
📊 ИТОГИ СЕССИИ

⏱️ Длительность: 12 мин
💬 Всего сообщений: 8
👤 Клиент: Иван Иванов
🌐 Язык: Russian
👨‍💼 Оператор подключался

Сессия завершена: 19.02.2026, 14:42
```

## 🔄 Когда задача закрывается

Задача автоматически закрывается в Планфиксе когда:

1. **Пользователь переключается обратно на AI** (команда `/ai` или `/bot`)
2. **Оператор закрывает сессию** через Telegram:
   - Кнопка "🔄 В AI / To AI"
   - Команда `/close [sessionId]`
3. **Сессия неактивна 5 минут** (автоматическое закрытие)

## 🛠️ Отключение интеграции

Если нужно временно отключить интеграцию, установите в `.env`:

```bash
PLANFIX_ENABLED=false
```

Сервер продолжит работать как обычно, но задачи в Планфикс создаваться не будут.

## ⚙️ Настройка API URL (опционально)

По умолчанию используется `https://api.planfix.ru`. Если у вас другой API endpoint, добавьте в `.env`:

```bash
PLANFIX_API_URL=https://your-custom-api.planfix.ru
```

## 🐛 Устранение неполадок

### Задачи не создаются

1. Проверьте, что `PLANFIX_ENABLED=true`
2. Проверьте правильность API токена
3. Проверьте ID проекта
4. Проверьте логи сервера на наличие ошибок:
   ```
   ❌ Failed to create Planfix task: HTTP 401: Unauthorized
   ```

### Ошибки авторизации (401)

- Проверьте, что API токен корректный
- Убедитесь, что токен не истёк
- Проверьте, что у токена есть права на создание задач

### Сообщения не логируются

- Проверьте, что задача успешно создалась (в логах должен быть `planfixTaskId`)
- Проверьте логи на ошибки при добавлении комментариев

### Типичные ошибки в логах:

```
⚠️ Planfix API token not configured - integration disabled
```
→ Не указан `PLANFIX_API_TOKEN` в `.env`

```
❌ Failed to create Planfix task: HTTP 404: Not Found
```
→ Неправильный ID проекта или аккаунт

```
❌ Failed to log message to Planfix: Connection refused
```
→ Проблемы с сетью или API недоступен

## 📝 Примечания

- Интеграция работает асинхронно и не блокирует чат
- Если запрос к Планфикс не удался, это не влияет на работу чата
- Все ошибки логируются в консоль сервера
- Задачи создаются с задержкой 2 секунды для сбора начальной информации

## 🔐 Безопасность

- ⚠️ **Не коммитьте `.env` файл в Git!** (уже добавлен в `.gitignore`)
- Храните API токен в безопасности
- Используйте HTTPS для API запросов (уже настроено)
- Регулярно проверяйте и обновляйте токены

## 📞 Поддержка

Если возникли проблемы с интеграцией:
1. Проверьте логи сервера
2. Убедитесь, что все настройки верны
3. Проверьте [документацию Planfix API](https://planfix.com/help/REST_API)

---

**Версия:** 1.0
**Последнее обновление:** 19.02.2026
